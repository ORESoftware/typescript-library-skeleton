{
  "version": 2,
  "jobs": {
    "build": {
      "docker": [
        {
          "image": "circleci/node:10"
        }
      ],
      "steps": [
          {
                "run":"npm set unsafe-perm true"
              },
        "checkout",
        {
          "run":"sudo npm install -g --loglevel=warn @oresoftware/r2g"
        },
       {
         "run": "sudo npm install --loglevel=warn"
         },
        {
          "run": "sudo r2g run"

        }
      ]
    }
  }
}
